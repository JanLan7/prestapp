{
  "name": "prestapp",
  "version": "1.0.0",
  "description": "Sistema de Gestión de Préstamos Profesional",
  "main": "server/index.js",
  "scripts": {
    "dev": "node --watch server/index.js",
    "build": "cd cliente && npm install && npm run build",
    "start": "node server/index.js",
    "install-all": "npm install && cd cliente && npm install"
  },
  "keywords": ["prestamos", "gestion", "inventario", "react", "node"],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.16.3"
  }
}
